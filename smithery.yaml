# Smithery AI Configuration for TestRail MCP Server
# Following official documentation for TypeScript runtime deployment

runtime: "typescript"

# Optional environment variables
env:
  NODE_ENV: "production"